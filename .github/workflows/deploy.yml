name: Deploy
on:
  push:
    branches:
    - main 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Deploy
        env:
          HEROKU_API_KEY: ${{0d0bae05-c9c5-4e37-ba48-07ebe65d3c43}}
          HEROKU_APP_NAME: "comp7940chatbo2023dy"  # Rename this to the app of your Heroku app.
        run: git push -f
https://heroku:$HEROKU_API_KEY@git.heroku.com/$HEROKU_APP_NAME.git main